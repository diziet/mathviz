#!/usr/bin/env bash
# Refuse merge commits on main. Skips silently when scripts/guard_main.sh is absent.
set -euo pipefail
guard="$(cd "$(dirname "$0")/.." && pwd -P)/scripts/guard_main.sh"
[ -f "$guard" ] || exit 0
. "$guard"
guard_main_commit
